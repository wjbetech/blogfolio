# Blogfolio TODO

Keep this file focused on the next few concrete steps only.

## Active

Key:

[ ] Task not started [-] Task in progress [x] Task finished

- [ ] Ensure content docs match the actual schema in `contentlayer.config.ts`
  - [ ] Audit `docs/contentlayer.md` against the current `Post` and `Project` schema
  - [ ] Audit `docs/content-operations.md` against the current required/optional frontmatter fields
  - [ ] Remove stale references to manual `slug` frontmatter where the schema no longer supports it
  - [ ] Correct route and content-type references so the docs match the current app surface
  - [ ] Manually verify the updated docs against one real post file and one real project file

- [ ] Add a lightweight docs audit cadence
  - [ ] Define when the docs audit should happen
  - [ ] Define which files must be checked during the audit
  - [ ] Define what should trigger an out-of-cycle docs update
  - [ ] Record the cadence in `docs/roadmap.md`
  - [ ] Add a short checklist entry to the content/documentation workflow docs

## After That

- [ ]

## Deferred

- [ ]
