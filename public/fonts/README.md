Place your Bricolage Grotesque font files here.

Expected filenames used by the app (update `src/app/layout.tsx` if you use different names):

- BricolageGrotesque-Regular.woff2
- BricolageGrotesque-Bold.woff2

Once these files are added, the site will load the local fonts automatically via `next/font/local`.
